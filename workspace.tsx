Task complete. Here's the summary:

## Result

**Changed file:** `app/routes/workspace.tsx`

**Changes made:**
- Replaced generic prose layout with brutalist tactical telemetry design
- `font-header` uppercase title "WORKSPACE" with `///` monospace subtitle
- Removed `prose dark:prose-invert max-w-none` wrapper
- Three sections (Hardware, Development Tools, Productivity) as `Compartment` bordered boxes
- Each section has a `SectionHeader` with `///` label
- List items in `font-mono text-sm` with `>>` accent arrows
- Data extracted into static arrays for readability
- `max-w-3xl` container constraint
- Meta title uppercase with em-dash format

**LSP warnings:** False positives (missing node_modules context in write tool) — same pattern as all other redesigned files.

**Progress updated:** `progress.md` now shows workspace.tsx as complete alongside journey.tsx.